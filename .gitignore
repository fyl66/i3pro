__pycache__/
*.pyc
out/
.pytest_cache/
*.parquet
*.delta.json
# 侧车文件：用户在场次上做的编辑（信标 / 数学通道 / 区段 / 注释 / GPS 校正）。
# 它们跟着 `.ld` 走，而 `.ld` 不进仓库，所以这些也不进——`git add -A` 时别把
# 某个人的手工编辑带上来。`maths/global.json` 是**跨场次复用**的全局定义，不在此列。
*.laps.json
*.maths.json
*.sections.json
*.notes.json
*.gps.json
*.map.json
# 侧车写盘时先落的临时文件（sidecar.py 原子替换用），正常情况下瞬间就没了。
*.json.tmp
.tmp_deps/
.venv/
venv/
node_modules/

# 试车数据与 MoTeC 官方文档不进仓库：
# 前者是 760 MB 的车队数据，后者是 MoTeC 的版权材料。
i2pro_data/
i2pro-help/
*.ld
*.ldx

# 代理干活时的临时草稿（提交信息、给票的验收草稿）：不是项目资产，别进仓库。
# `_commit_msg.txt` 曾经被误提交进 06e31dd，这里补上规则免得再来一次。
_commit_msg.txt
_issue*.md
