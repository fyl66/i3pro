# i3pro

把车队 MoTeC C125 记录仪产出的 `.ld` 日志，变成"点开就能用、能按赛道距离比圈、能发给队友"的本地分析工具。
这份词汇表只记录**这个项目自己的概念**——分析工作里的人和判断。一般编程概念（超时、错误类型、工具函数）不在此列。

## Language

### 数据

**场次 / session**:
一个 `.ld` 日志文件，用文件名（不含扩展名）标识。
_Avoid_: 文件, log, outing

**通道 / channel**:
日志里的一路信号，带单位、采样率、小数位与显示精度。
_Avoid_: 变量, 信号, 参数

**数学通道 / maths channel**:
由一条白名单表达式算出来的通道。它**没有**被记录过，所以没有原始整数样本，
只有主时间基上的浮点列；除此之外与原生通道完全一样（可画图、可散点、可切圈、可进报表）。
_Avoid_: 派生量, 计算列, 虚拟通道

**作用域 / scope**:
一条数学通道定义能影响谁。**本地**跟着一个场次（存进那个场次的侧车），**全局**跟着工具
（存进仓库的 `maths/global.json`），同名时本地赢。
_Avoid_: 层级, 级别, 命名空间

**分组 / group**:
单位相同的若干通道，因而共享同一条纵轴。
_Avoid_: 图层, 面板组

**侧车文件 / sidecar**:
与场次同名、存放用户改动（信标、切分方式、可信圈）的 JSON 文件。`.ld` 本身永远只读。
_Avoid_: 配置文件, 缓存

**金标准数据 / golden data**:
用来判定"改完还能不能用"的两场真实试车数据——`高避5圈` 验功能，`耐久正赛` 验性能。
_Avoid_: 测试数据, 样例数据

### 切圈

**信标 / beacon**:
**一次穿越起终点的记录**。它有一个位置（起终点线画在哪）和一个时刻（什么时候穿过去的），
两者都是它的属性而不是它的种类。自动检出给出时刻、手工放置给出位置，漏掉的那次可以只给时刻。
一个信标对应一条独立的圈速序列。
_Avoid_: 门, gate, 标记点, 检查点

**圈 / lap**:
**同一个信标相邻两次穿越之间的区间**。永远不要用它指"一段距离"，也不要指几何上的一个环。
_Avoid_: 段, 环, loop

**环 / loop**:
八字这类自交赛道上的一个几何回环（左环、右环）。环是**形状**，不是计时单位；
要得到"左环的第 n 圈"，就在左环上放一个信标。
_Avoid_: 圈, 左圈, 右圈

**运行 / run**:
一次尝试：从车起步到再次停稳之间的持续行驶。八字、直线加速、skidpad 这些没有圈的测试，
有意义的单位是运行而不是圈。
_Avoid_: 趟, 轮次, attempt

### 界面

**组件 / component**:
工作表上的一块面板：时间/距离图、散点图、赛道轨迹、仪表、时间差、状态与故障。
_Avoid_: 图表, panel, widget

**工作表 / worksheet**:
组件的网格布局，连同它的分享链接。一个页面就是一张工作表。
_Avoid_: 布局, dashboard

**快照 / snapshot**:
把某一场次连同当时的工作表导出成一个自包含 HTML——不需要 Python、不需要服务器、不需要联网。
_Avoid_: 导出, 静态页

**本地服务 / serve**:
把工作台跑成本地或局域网服务，缩放时按可见区间重新取全分辨率数据。
_Avoid_: 后端, 在线模式

**最快圈差 / delta to best**:
某一圈的圈速与本次最快圈速之差，一个标量（秒）。圈速表里的那一列。
_Avoid_: Δ, 差值

**圈差曲线 / lap delta curve**:
两条圈在距离轴上逐点的累计时间差，是距离的函数。双圈对比图下面那条红绿填充曲线。
_Avoid_: Δ, delta, 时间差
