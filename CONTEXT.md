# i3pro

把车队 MoTeC C125 记录仪产出的 `.ld` 日志，变成"点开就能用、能按赛道距离比圈、能发给队友"的本地分析工具。
这份词汇表只记录**这个项目自己的概念**——分析工作里的人和判断。一般编程概念（超时、错误类型、工具函数）不在此列。

## Language

### 数据

**场次 / session**:
一个 `.ld` 日志文件，用文件名（不含扩展名）标识。
_Avoid_: 文件, log, outing

**通道 / channel**:
日志里的一路信号，带单位、采样率、小数位与显示精度。
_Avoid_: 变量, 信号, 参数

**数学通道 / maths channel**:
由一条白名单表达式算出来的通道。它**没有**被记录过，所以没有原始整数样本，
只有主时间基上的浮点列；除此之外与原生通道完全一样（可画图、可散点、可切圈、可进报表）。
_Avoid_: 派生量, 计算列, 虚拟通道

**作用域 / scope**:
一条数学通道定义能影响谁。**本地**跟着一个场次（存进那个场次的侧车），**全局**跟着工具
（存进仓库的 `maths/global.json`），同名时本地赢。
_Avoid_: 层级, 级别, 命名空间

**分组 / group**:
单位相同的若干通道，因而共享同一条纵轴。
_Avoid_: 图层, 面板组

**侧车文件 / sidecar**:
与场次同名、存放用户改动（信标、切分方式、可信圈）的 JSON 文件。`.ld` 本身永远只读。
_Avoid_: 配置文件, 缓存

**金标准数据 / golden data**:
用来判定"改完还能不能用"的两场真实试车数据——`高避5圈` 验功能，`耐久正赛` 验性能。
_Avoid_: 测试数据, 样例数据

### 切圈

**信标 / beacon**:
**一次穿越起终点的记录**。它有一个位置（起终点线画在哪）和一个时刻（什么时候穿过去的），
两者都是它的属性而不是它的种类。自动检出给出时刻、手工放置给出位置，漏掉的那次可以只给时刻。
一个信标对应一条独立的圈速序列。
_Avoid_: 门, gate, 标记点, 检查点

**圈 / lap**:
**同一个信标相邻两次穿越之间的区间**。永远不要用它指"一段距离"，也不要指几何上的一个环。
_Avoid_: 段, 环, loop

**环 / loop**:
八字这类自交赛道上的一个几何回环（左环、右环）。环是**形状**，不是计时单位；
要得到"左环的第 n 圈"，就在左环上放一个信标。
_Avoid_: 圈, 左圈, 右圈

**运行 / run**:
一次尝试：从车起步到再次停稳之间的持续行驶。八字、直线加速、skidpad 这些没有圈的测试，
有意义的单位是运行而不是圈。
_Avoid_: 趟, 轮次, attempt

**赛道区段 / track section**:
沿赛道**距离**量出来的一段（弯道或直道），边界以米记在一条参考圈上，套在每一条圈上使用；
它是"哪里算弯"，与时间无关。一半靠自动切分给出，改过之后由人说了算。
_Avoid_: 段, 分段, sector（那说的是计时段，不是沿距离切出来的区段）

**分段计时 / section time**:
一条圈在**某一个赛道区段**里用掉的时间（秒）。它属于"某条圈 × 某个区段"这一格，
是所有圈在同一条区段上的第一次时间比较。**分段是"用时"，区段是"距离区间"**，
别把两者混成一个词。
_Avoid_: 段时间, 区间用时

**理论最快圈 / eclectic best lap**:
每个区段各自的最快**分段计时**相加得到的圈速（秒）。它把不同圈的最好一段拼在一起，
现实中不可能每段同时最快——它是一条**参考下限**，不是一条真跑出来的圈。只取完整圈的段。
_Avoid_: 最佳圈, 理想圈, 最快圈

**连续最快圈 / rolling best lap**:
在一整段连续行驶的数据上滑一个"一圈长度"的窗口，用时最短的那一个。窗口按**距离**滑，
两端都取**首次到达**该距离的时刻，所以它会跨过起点线，也把窗口里停着的秒数算进去。
与"最快圈"（两条信标之间的那段）不是一个概念，两个都要显示。
_Avoid_: 最快圈, 滚动圈

### 界面

**组件 / component**:
工作表上的一块面板：时间/距离图、散点图、赛道轨迹、仪表、时间差、状态与故障、
时间报告、通道报告。
_Avoid_: 图表, panel, widget

**时间报告 / time report**:
"区段 × 圈"的分段计时矩阵，外加理论最快圈与连续最快圈的那张表。一行是区段、一列是一条圈。
_Avoid_: 报表, 计时报告

**通道报告 / channel report**:
一行是"某个分组条目 × 某条通道"、列是八项统计量（最小/最大/绝对最大/均值/起值/终值/
变化量/标准差）的那张表。分组条目可以是圈，也可以是某个区段。
_Avoid_: 统计表, 汇总表

**工作表 / worksheet**:
组件的网格布局，连同它的分享链接。一个页面就是一张工作表。**具名工作表**是仓库里
`worksheets/*.json` 的一个文件（文件名是身份、`name` 是按钮上的字），顶上那排按钮就是
它们；页面里正在用的那份布局记在浏览器本地，**按场次分**。
_Avoid_: 布局, dashboard, 预设（"预设"是具名工作表的老叫法，别再用）

**挑通道 / pick**:
工作表里说"这个组件要哪几类通道"的那条规则（`patterns` / `limit` / `index` / `special`），
打开某个场次时用**那个场次真实存在的通道**去填。写死通道名会让工作表换个场次就整片空。
_Avoid_: 通道匹配, 自动选通道

**快照 / snapshot**:
把某一场次连同当时的工作表导出成一个自包含 HTML——不需要 Python、不需要服务器、不需要联网。
_Avoid_: 导出, 静态页

**本地服务 / serve**:
把工作台跑成本地或局域网服务，缩放时按可见区间重新取全分辨率数据。
_Avoid_: 后端, 在线模式

**最快圈差 / delta to best**:
某一圈的圈速与本次最快圈速之差，一个标量（秒）。圈速表里的那一列。
_Avoid_: Δ, 差值

**圈差曲线 / lap delta curve**:
两条圈在距离轴上逐点的累计时间差，是距离的函数。双圈对比图下面那条红绿填充曲线。
_Avoid_: Δ, delta, 时间差
