refactor(viewer): 显示形式全部迁进注册表，取数收成一条路径（ticket #19 #20 #21）

每种显示形式（图 / 散点 / 直方图 / 频谱 / 轨迹 / 仪表 / 时间报告 / 通道报告）在
COMPONENT_TYPES 里只声明一次自己：默认配置、标题、要哪些通道、控件条、画布事件、
下拉填值、刚加进来先看什么、侧边栏通道清单归谁、怎么进分享链接、怎么画、怎么取数。
工作表按这些声明装配，不再挨个类型判断——`type === "…"` 从 80 处降到 10 处，剩下
的 10 处全是语义判断（9 处"哪个组件是图" + 1 处"哪一列是文字列"）。注册表里那条
自检形式（`caption`）仍然只证明"加一种显示形式 = 一条声明"。

取数只有一条路：地址由 apiUrl 拼、请求由 apiGet 发、出错在那里统一成一句话；每个
组件的结果放在**它自己的槽**里（bundle.data / bundle.data2），键由它自己的
data.key 算，删掉组件时槽跟着没。散点原来把结果塞进全表共用的 state.points，两张
散点同屏会互相顶掉——这一票就是修它的。六条手工取数（trace / points / histogram /
spectrum / track / report）里，五个组件端点的 fetch 全部消失；`/at` 与 `/overlay`
两条数据读取也改走同一个出口（写请求 PUT/POST 与侧车读写不在这一票里）。

验证：220 项单测 OK；verify_ld_vs_csv PASS（0 channel(s) outside tolerance）；
两份金标准快照 smoke 均 PASS（无头断言点 372 → 397）；真浏览器真鼠标
tools/verify_clicks.py 51/51 通过。验收条目见 docs/ACCEPTANCE.md 的 A41 / A42。
