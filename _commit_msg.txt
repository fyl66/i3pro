test(viewer): 两张直方图同屏互不顶掉的断言（ticket #21）

#21 的验收条目点名了两对：两张散点、两张直方图（不同分箱数）。上一提交里两种
都改成了「各拿自己那一份」，但只有散点那对写了断言。这一条把直方图那对也钉住：
各塞一份 11 格与 33 格的分布，重画后各自报自己的通道与点数、缓存键不同，从工作表
里拿掉之后数据槽不残留。

断言点 397 → 402；单测 220 项 OK；两份金标准快照 smoke 均 PASS。
